<Page
    x:Class="WebView.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" SizeChanged="Grid_SizeChanged">
        <WebView x:Name="webView1" Source="https://www.netflix.com/"/>
        <CommandBar x:Name="CommandBar" Background="#7FE6E6E6">
            <AppBarButton x:Name="FocuseMode" Click="Focus_Click" Label="Fullscreen">
                <AppBarButton.Icon>
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE1D9;" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton x:Name="MiniMode" Click="MiniView_Click" Label="Fullscreen">
                <AppBarButton.Icon>
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8A7;" />
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>
    </Grid>
</Page>
